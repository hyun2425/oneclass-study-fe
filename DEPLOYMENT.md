# GitHub Pages 배포 가이드

이 프로젝트를 GitHub Pages에 배포하는 방법을 안내합니다.

## 📋 사전 준비

1. GitHub 계정이 있어야 합니다
2. 프로젝트가 GitHub 리포지토리에 푸시되어 있어야 합니다

## 🚀 배포 방법

### 방법 1: GitHub Actions (자동 배포) - 추천 ⭐

가장 간단하고 자동화된 방법입니다.

#### 1단계: GitHub 리포지토리 설정

1. GitHub에서 리포지토리로 이동
2. **Settings** → **Pages** 메뉴로 이동
3. **Source**를 **GitHub Actions**로 선택
4. 저장

#### 2단계: 리포지토리 이름 확인 및 수정

1. 리포지토리 이름을 확인하세요 (예: `onclass-study-fe`)
2. `vite.config.ts` 파일에서 `repositoryName` 변수를 확인/수정:
   ```typescript
   const repositoryName = 'onclass-study-fe'  // 여기를 리포지토리 이름으로 수정
   ```

#### 3단계: 코드 푸시

```bash
git add .
git commit -m "Add GitHub Pages deployment"
git push origin main
```

#### 4단계: 배포 확인

1. GitHub 리포지토리의 **Actions** 탭으로 이동
2. 워크플로우가 실행되는지 확인
3. 완료되면 **Settings** → **Pages**에서 배포된 URL 확인
   - 예: `https://[사용자명].github.io/onclass-study-fe/`

### 방법 2: gh-pages 패키지 사용 (수동 배포)

#### 1단계: gh-pages 패키지 설치

```bash
npm install --save-dev gh-pages
```

#### 2단계: 배포 실행

```bash
npm run deploy
```

이 명령어는:
1. 프로젝트를 빌드합니다 (`npm run build`)
2. `dist` 폴더를 `gh-pages` 브랜치에 푸시합니다

#### 3단계: GitHub Pages 설정

1. GitHub 리포지토리 → **Settings** → **Pages**
2. **Source**를 **Deploy from a branch**로 선택
3. **Branch**를 `gh-pages`로 선택
4. **Folder**를 `/ (root)`로 선택
5. 저장

## ⚙️ 설정 파일 설명

### vite.config.ts

```typescript
base: process.env.NODE_ENV === 'production' ? `/${repositoryName}/` : '/'
```

- 프로덕션 빌드 시 리포지토리 이름을 base path로 사용
- 개발 환경에서는 `/` 사용

### .github/workflows/deploy.yml

GitHub Actions 워크플로우 파일:
- `main` 브랜치에 푸시할 때마다 자동으로 빌드 및 배포
- 수동으로도 실행 가능 (workflow_dispatch)

## 🔧 문제 해결

### 1. 404 에러가 발생하는 경우

- `vite.config.ts`의 `repositoryName`이 리포지토리 이름과 일치하는지 확인
- GitHub Pages 설정에서 올바른 브랜치/폴더가 선택되었는지 확인

### 2. 라우팅이 작동하지 않는 경우

React Router를 사용하는 경우, GitHub Pages는 SPA를 지원하지 않으므로:
- 모든 경로를 `index.html`로 리다이렉트하는 설정이 필요할 수 있습니다
- 현재 설정으로는 대부분의 경우 정상 작동합니다

### 3. 빌드가 실패하는 경우

- GitHub Actions의 로그를 확인하세요
- 로컬에서 `npm run build`가 성공하는지 확인하세요

## 📝 참고사항

- 배포된 사이트는 `https://[사용자명].github.io/[리포지토리명]/` 형식의 URL을 가집니다
- 커스텀 도메인을 사용하려면 GitHub Pages 설정에서 도메인을 추가할 수 있습니다
- 배포에는 몇 분 정도 소요될 수 있습니다

## 🎉 완료!

배포가 완료되면 GitHub Pages URL로 접속하여 사이트를 확인할 수 있습니다!

